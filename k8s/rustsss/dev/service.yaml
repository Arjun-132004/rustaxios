apiVersion: v1
kind: Service
metadata:
  name: rustaxios
  namespace: storefront-devrus
  labels:
    app.kubernetes.io/name: rustaxios
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rustaxios
  ports:
    - name: http
      port: 80
      targetPort: 8080
